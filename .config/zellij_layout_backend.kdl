layout {
    pane split_direction="vertical" {
      pane split_direction="horizontal" {
        pane {
          command "make"
          args "build-libs"
        }
        pane {
          command "make"
          args "build-backend"
        }
      }

      pane {
        pane {
          pane {
            command "make"
            args "run-backend"
          }
        }
      }
    }
}