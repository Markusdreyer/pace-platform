// # WebSocket Endpoints
// WS /events/{event_id}/participants/{participant_id}/location # WebSocket endpoint for real-time location updates
// WS /events/{event_id}/public_data # WebSocket endpoint for real-time public data updates
